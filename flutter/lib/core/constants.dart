class AppConstants {
  // TODO: Change this IP before running on a physical device.
  // Use 10.0.2.2 for emulator, or your PC's local IP (run `ipconfig` on Windows)
  // for a real phone. Both devices must be on the same Wi-Fi network.
  static const String baseUrl = 'http://10.0.2.2:3000';
}
